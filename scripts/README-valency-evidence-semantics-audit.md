# Valency evidence semantics audit command

Run the source-evidence diagnostic with:

```sh
npx tsx scripts/audit-valency-evidence-semantics.ts <ud-syntax-evidence.json>
```

The command is audit-only. It does not modify generated artifacts or runtime profiles.
