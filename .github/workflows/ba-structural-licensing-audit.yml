name: BA structural licensing audit

on:
  push:
    branches:
      - agent/ba-structural-licensing-audit
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci --include=dev --ignore-scripts
      - name: Build app catalog
        run: npm run app:catalog
      - name: Audit BA structural licensing
        run: npx tsx scripts/audit-ba-structural-licensing.ts | tee /tmp/ba-structural-licensing.json
      - name: Upload audit
        uses: actions/upload-artifact@v4
        with:
          name: ba-structural-licensing-audit
          path: /tmp/ba-structural-licensing.json
          if-no-files-found: error
